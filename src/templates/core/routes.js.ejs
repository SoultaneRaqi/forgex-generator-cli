import { Router } from 'express';

// 1. Import module routes here
// Example: import userRoutes from '../modules/users/user.routes.js';

const router = Router();

// 2. Register module routes here
// Example: router.use('/users', userRoutes);

export default router;